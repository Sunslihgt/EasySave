<Window x:Class="EasySave.Views.LanguageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySave.Views"
        mc:Ignorable="d"
        Title="{Binding Path=Title}" Height="300" Width="400" Background="#f0f0f0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Path=ChooseLanguageText}" 
                   Style="{StaticResource LanguageWindowTextBlockStyle}"/>

        <ComboBox x:Name="LanguageComboBox" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                  Style="{StaticResource LanguageComboBoxStyle}">
            <ComboBoxItem Content="Français" Tag="FR"/>
            <ComboBoxItem Content="English" Tag="EN"/>
            <ComboBoxItem Content="Español" Tag="ES"/>
        </ComboBox>
        
        <Button x:Name="SubmitButton" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Content="{Binding Path=ModifyLanguageButtonText}" Width="150" Height="30"
                HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20,0,20"
                Style="{StaticResource RoundedButtonStyle}" Click="SubmitButton_Click"/>
    </Grid>
</Window>

